# Analytics Service Dependencies
# ================================
# 
# INSTALLATION OPTIONS:
# 
# Option 1: CPU Only (slower, easier)
#   pip install -r requirements-cpu.txt
#
# Option 2: GPU with CUDA 11.8
#   pip install -r requirements-gpu-cu118.txt
#
# Option 3: GPU with CUDA 12.1
#   pip install -r requirements-gpu-cu121.txt
#
# Option 4: GPU with CUDA 12.4 (Latest)
#   pip install -r requirements-gpu-cu124.txt
#
# Option 5: Manual Installation (if above don't work)
#   1. Install PyTorch first: https://pytorch.org/get-started/locally/
#   2. pip install -r requirements-base.txt
#
# ================================

# Default: CPU installation
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Async Kafka
aiokafka>=0.10.0

# High-performance async event loop (Linux/macOS only)
uvloop>=0.19.0; sys_platform != "win32"

# Data validation
pydantic>=2.5.0

# Configuration
PyYAML>=6.0.1

# Logging
loguru>=0.7.2

# Utilities
python-dateutil>=2.8.2
numpy>=1.24.0

# Computer Vision
opencv-python>=4.8.0

# Object Detection
ultralytics>=8.0.0

# Depth Estimation (Hugging Face Transformers)
transformers>=4.36.0
pillow>=10.0.0

# Development (optional)
pytest>=7.4.0
pytest-asyncio>=0.23.0
